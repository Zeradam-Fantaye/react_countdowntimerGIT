import React from 'react';
import ReactDOM from 'react-dom'; //Controls throwing the react code onto the browswer
import App from './app'

ReactDOM.render(
    //Where in the HTML to render this document
    <App />, document.getElementById("root")
);